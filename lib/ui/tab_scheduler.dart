import 'package:flutter/material.dart';

class TabScheduler extends StatelessWidget {
  const TabScheduler({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('스케줄러')),
    );
  }
}