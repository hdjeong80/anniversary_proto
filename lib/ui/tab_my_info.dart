import 'package:flutter/material.dart';

class TabMyInfo extends StatelessWidget {
  const TabMyInfo({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('내 정보')),
    );
  }
}